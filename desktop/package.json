{
  "name": "ulnclaw-desktop",
  "productName": "ulnclaw",
  "private": true,
  "version": "0.3.0",
  "description": "Tauri desktop shell for the ulnclaw agent (gateway-backed chat UI).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
